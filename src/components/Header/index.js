import React from "react";
import Nav from "../Nav";
import Logo from "../Logo";
import MiniLogo from "../MiniLogo";

const Header = () => {
  return (
    <header>
      <Nav />
      <Logo />
      <MiniLogo />
    </header>
  );
};

export default Header;
